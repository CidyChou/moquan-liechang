# 墨圈猎场 · 对战 MVP 任务拆分

状态：已开工（PM 2026-09-06）  
PRD：`/workspace/墨圈猎场/MVP-PRD-对战.md`（已确认）  
单机 Demo：`/workspace/ink-hunt/index.html`

## TBD 可调默认（先落地，测后改）

| 项 | 默认 | 说明 |
|---|---|---|
| 命数 N | 3 | 开局命数 |
| 击杀目标 K | 30 | 先到者胜 |
| 塞敌冷却 | 0.3s | 两次塞敌最短间隔 |
| 塞敌场上上限 | 对手本底外额外怪 ≤ 20 | 超限则排队/丢弃最旧策略自定，须可配置 |
| 落后补偿 | 分差≥8 时移速 +15%，分差清零后取消 | 至少一种；可换护盾等 |

断线：首版「30s 无心跳判负 / 匹配 15s 超时回大厅」简单处理即可。

## 进度节点

1. **方案对齐**（设计师 + 前后端）：UI 框 + 前后端接口草约  
2. **可匹配可打**：两人匹配进房，各打各图，比分可见  
3. **塞敌 + 升级 + 复活**：验收 3/5/6  
4. **反滚雪球 + 广告占位**：验收 7/8  
5. **部署 H5 地址**：验收 9  
6. **策划对照 PRD §9 体验拍板**

## 分派

### 设计师
- 匹配/大厅、局内 HUD（比分/击杀进度）、三选一、命尽 5s 复活（含广告占位）、结算页（含广告占位）
- 交付：可给前端落地的结构说明 + 关键标注（不必出完整视觉精稿也可，但交互与占位要清）

### 牛马--后端
- Node.js：匹配、房间、权威状态（击杀/塞敌/命/复活/胜负）
- 塞敌：A 杀 1 → B 立刻 +1 同种；冷却与上限可配置
- 落后补偿可配置；TBD 默认见上表
- 与前端约定：WS 或等价实时协议；文档放同目录

### 牛马--前端
- 基于 Demo 做 H5 Canvas：本底怪 + 三选一对齐 Demo
- 各打各图；对接匹配与同步；HUD/复活/结算按设计落地
- 广告位可点击走通，不接 SDK

### 牛马--运维
- 部署 Node + 静态 H5，输出公网可访问地址
- HTTPS/反代按需；配合前后端启动方式

### 牛马--测试
- 按 PRD §9 写验收清单与双端复现步骤
- 节点 2 起可冒烟；节点 5 后全面回归；结果回 PM

## 不做（本版）
限时/生存、房间码、真广告 SDK、小游戏、观战小窗、排行榜等（见 PRD §3）

## 单机 Demo 路径（策划补充）

1. 推荐工作副本：`/workspace/ink-hunt/index.html`
2. 原始附件：`/home/box/agent-data/agents/56969c5d-40dd-4a69-81cd-82a69d00a83d/attachments/382d502b5257feb245fa026541fa4c6e66b1d9049f5910b705358b855f32071b.html`
3. 公网试玩可能失效：`https://eighty-pants-lead.loca.lt`（以本地为准）
无单独仓库。

## 交付边界（boss 更正）

- 可本地跑说明 → 回 PM → 回 boss
- **最终公网 H5 地址由 boss 部署交付用户**
- 运维：本机/内网跑通即可，不抢公网交付
